import Sidebar from "@/components/layout/sidebar";
import EventSection from "@/components/widgets/event-section";

const page = ({}) => {
  return <EventSection></EventSection>;
};

export default page;
